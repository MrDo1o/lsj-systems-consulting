---
import astroLogo from '../assets/astro.svg';
---

<div id="container">
	<main>
		<section id="hero" class="reveal">
			<div class="hero-copy">
				<a class="brand" href="https://astro.build"
					><img src={astroLogo.src} width="115" height="48" alt="Astro Homepage" /></a
				>
				<h1>
					To get started, open the <code>src/pages</code> directory in your project.
				</h1>
				<p>
					Build consulting engagements with a delivery-first operating model that keeps strategy,
					execution, and architecture aligned.
				</p>
				<section id="links">
					<a class="button" href="https://docs.astro.build">Read our docs</a>
					<a href="https://astro.build/chat"
						>Join our Discord <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 127.14 96.36"
							><path
								fill="currentColor"
								d="M107.7 8.07A105.15 105.15 0 0 0 81.47 0a72.06 72.06 0 0 0-3.36 6.83 97.68 97.68 0 0 0-29.11 0A72.37 72.37 0 0 0 45.64 0a105.89 105.89 0 0 0-26.25 8.09C2.79 32.65-1.71 56.6.54 80.21a105.73 105.73 0 0 0 32.17 16.15 77.7 77.7 0 0 0 6.89-11.11 68.42 68.42 0 0 1-10.85-5.18c.91-.66 1.8-1.34 2.66-2a75.57 75.57 0 0 0 64.32 0c.87.71 1.76 1.39 2.66 2a68.68 68.68 0 0 1-10.87 5.19 77 77 0 0 0 6.89 11.1 105.25 105.25 0 0 0 32.19-16.14c2.64-27.38-4.51-51.11-18.9-72.15ZM42.45 65.69C36.18 65.69 31 60 31 53s5-12.74 11.43-12.74S54 46 53.89 53s-5.05 12.69-11.44 12.69Zm42.24 0C78.41 65.69 73.25 60 73.25 53s5-12.74 11.44-12.74S96.23 46 96.12 53s-5.04 12.69-11.43 12.69Z"
							></path></svg
						>
					</a>
				</section>
			</div>

			<aside class="proof box" aria-label="Client outcomes">
				<h2>Proof points</h2>
				<ul>
					<li><strong>32%</strong> faster release cadence in the first quarter.</li>
					<li><strong>2x</strong> improvement in cross-team delivery predictability.</li>
					<li><strong>99.95%</strong> uptime sustained through architecture modernization.</li>
				</ul>
			</aside>
		</section>

		<section id="services" class="reveal">
			<h2>Advisory Services</h2>
			<div class="service-grid">
				<article class="box service-card">
					<h3>Systems Strategy</h3>
					<p>Clarify priorities, map system constraints, and sequence investments around outcomes.</p>
				</article>
				<article class="box service-card">
					<h3>Delivery Enablement</h3>
					<p>Improve team flow with practical governance, planning rhythms, and operating metrics.</p>
				</article>
				<article class="box service-card">
					<h3>Architecture Review</h3>
					<p>Assess platform risk and define targeted changes that increase resilience and speed.</p>
				</article>
			</div>
		</section>
	</main>
</div>

<style>
	:root {
		--font-display: 'Inter', 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
		--font-body: 'Inter', 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
		--size-display: clamp(1.8rem, 2.5vw, 2.6rem);
		--lh-display: 1.2;
		--size-body: 1rem;
		--lh-body: 1.6;
		--size-label: 0.9rem;
		--neutral-0: #f5f5f4;
		--neutral-50: #fafaf9;
		--neutral-100: #f1f0ee;
		--neutral-700: #44403c;
		--neutral-900: #1c1917;
		--accent: #1d4ed8;
		--border: #d6d3d1;
	}

	#container {
		font-family: var(--font-body);
		background: var(--neutral-0);
		color: var(--neutral-900);
		min-height: 100%;
	}

	main {
		max-width: 1080px;
		margin: 0 auto;
		padding: clamp(1rem, 3vw, 3rem);
		display: grid;
		gap: clamp(1.5rem, 2.5vw, 3rem);
	}

	#hero {
		display: grid;
		gap: 1.25rem;
		grid-template-columns: 1.2fr 1fr;
		align-items: start;
	}

	.brand {
		display: inline-flex;
	}

	h1 {
		font-family: var(--font-display);
		font-size: var(--size-display);
		line-height: var(--lh-display);
		margin: 0.4rem 0 0;
		letter-spacing: -0.02em;
	}

	p,
	li {
		font-size: var(--size-body);
		line-height: var(--lh-body);
		color: var(--neutral-700);
		margin: 0;
	}

	#links {
		display: flex;
		gap: 0.8rem;
		margin-top: 1rem;
		flex-wrap: wrap;
	}

	#links a {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.65rem 0.95rem;
		text-decoration: none;
		border-radius: 0.6rem;
		font-size: var(--size-label);
		font-weight: 600;
		color: var(--neutral-900);
		transition: transform 0.2s ease, background-color 0.2s ease, color 0.2s ease;
	}

	#links a.button {
		background: var(--accent);
		color: #fff;
	}

	#links a.button:hover {
		transform: translateY(-1px);
		background: #1e40af;
	}

	#links a:not(.button):hover {
		background: var(--neutral-100);
	}

	#links a svg {
		height: 1em;
	}

	.box {
		background: var(--neutral-50);
		border: 1px solid var(--border);
		border-radius: 0.9rem;
		padding: 1rem;
	}

	.proof h2,
	#services h2 {
		margin: 0 0 0.9rem;
		font-size: 1.2rem;
	}

	.proof ul {
		margin: 0;
		padding-left: 1.15rem;
		display: grid;
		gap: 0.85rem;
	}

	.proof strong {
		color: var(--accent);
	}

	#services {
		display: grid;
		gap: 1rem;
	}

	.service-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 0.9rem;
	}

	h3 {
		margin: 0 0 0.5rem;
		font-size: 1rem;
		color: var(--neutral-900);
	}

	code {
		font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', monospace;
		background: var(--neutral-100);
		border: 1px solid var(--border);
		border-radius: 0.5rem;
		padding: 0.2rem 0.45rem;
	}


	.reveal {
		animation: reveal-up 0.55s ease-out both;
	}

	#services.reveal {
		animation-delay: 0.08s;
	}

	@keyframes reveal-up {
		from {
			opacity: 0;
			transform: translateY(8px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@media (max-width: 900px) {
		#hero {
			grid-template-columns: 1fr;
		}

		.service-grid {
			grid-template-columns: 1fr;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.reveal,
		#links a {
			animation: none;
			transition: none;
		}

		#links a.button:hover {
			transform: none;
		}
	}
</style>
